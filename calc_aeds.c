#include <stdio.h>
#include <stdlib.h>
  void main () {

    int base_antes, base_depois, n_antes, n_depois;

    printf("Digite o número que deseja converter e sua base\n");
    scanf("%d %d", &n_antes, &base_antes);
    printf("Digite a base para que deseja converter\n");
    scanf("%d", &base_depois);

    
    }
